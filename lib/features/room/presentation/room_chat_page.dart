import 'package:flutter/material.dart';

class RoomChatPage extends StatelessWidget {
  const RoomChatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Chat de la room'));
  }
}
