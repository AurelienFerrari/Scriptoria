# Plan de correction des bogues

Bogues détectés au fil du développement (revue de code, écriture des tests,
CI) et leur statut. Les IDs sont référencés depuis le [cahier de recettes](CAHIER_DE_RECETTES.md).

## Corrigés

| ID | Bogue | Gravité | Détecté par | Correctif |
|---|---|---|---|---|
| B01 | Conflit Git non résolu (marqueurs `<<<<<<<`/`=======`/`>>>>>>>`) resté commité dans `README.md` | Mineure | Revue de code | Résolution manuelle du conflit — commit `6a1b5f4` |
| B02 | Mot de passe stocké en double : Supabase Auth (sécurisé) **+** un second hash SHA-256 non salé dans `users.password_hash`, jamais relu | Critique (sécurité, OWASP A02) | Revue de code lors de la recréation du schéma | Suppression du second hash, dépendance `crypto` retirée — commit `6a1b5f4` |
| B03 | Crash garanti sur le build web : `Platform.environment` (`dart:io`) n'existe pas sur Flutter Web | Critique | Revue de code avant mise en place de la CI web | Appel protégé par `kIsWeb` — commit `e068294` |
| B04 | `createUserProfile()` recevait l'id utilisateur mais ne l'insérait jamais dans la table `users` : avec les RLS actives, chaque inscription échouait silencieusement (erreur avalée par un `catch` sans `rethrow`) | Critique | Conception du schéma Supabase + RLS | Ajout de `'id': userId` dans l'insertion |
| B05 | Crash de navigation dans une room : `RoomNavbar` affichait 5 onglets mais `RoomShell` ne branchait que 3 pages (`RangeError`) | Critique | Revue de code (avant fusion de la branche `register`) | Résolu de fait par la fusion de `register`, qui refaisait `RoomShell` avec 5 pages |
| B06 | `_loadUserProfile()` appelait `Navigator.pushReplacementNamed` de façon synchrone depuis `initState()` (avant tout `await`) : lève `setState() called during build` pour un utilisateur non connecté ouvrant son profil | Majeure | Test automatisé (`profile_page_test.dart`) | Navigation reportée via `WidgetsBinding.instance.addPostFrameCallback` |
| B07 | Version de NDK Android figée en dur (`27.0.12077973`) : le SDK manager la re-téléchargeait à chaque run CI, et le zip arrivait parfois corrompu, faisant échouer le build APK | Majeure (CI) | Échec du job CI "Build APK" | `ndkVersion = flutter.ndkVersion` (version déjà en cache) |
| B08 | Méthode de calcul du taux de couverture de tests erronée (division par le nombre brut de lignes du fichier au lieu des lignes exécutables identifiées par `lcov`), plafonnant artificiellement le pourcentage bien avant 50% | Majeure (fiabilité de la mesure qualité) | Relecture de la méthodologie suite à une remarque | Recalcul en LH/LF cumulé sur `lib/`, seuil CI corrigé |
| B09 | Deux icônes sous le seuil de contraste WCAG de 3:1 (`white30` à 2.70:1, `white38` à 2.60:1) | Mineure (accessibilité) | Calcul systématique des contrastes de la palette | Relevées à `white54` (4.46:1 et 4.66:1) |
| B10 | Mot de passe accepté dès 6 caractères (sous la recommandation OWASP/NIST de 8) | Mineure (sécurité) | Revue de code | Seuil relevé à 8 caractères |
| B11 | Warnings d'analyse statique : imports dupliqués (`room_tools_page.dart`), imports inutilisés (`room_shell.dart`, `main.dart`), champs de formulaire assignés mais jamais lus (`room_create_page.dart`, `room_join_page.dart`), comparaison null devenue impossible (`supabase_service.dart`) | Mineure | `flutter analyze` en CI | Nettoyage du code — commit `9358aa2` |
| B12 | Le lien « Mot de passe oublié ? » de `LoginPage` pointait vers la route `/forgot-password`, jamais déclarée dans `main.dart` | Majeure | Cahier de recettes (R05) | Nouvel écran `ForgotPasswordPage` (appelle `AuthProvider.resetPassword`, déjà présent côté service mais jamais exposé) + route ajoutée — commit `83ee877` |
| B13 | `RoomCreatePage` ne persistait rien : le bouton « Créer » affichait un SnackBar puis revenait en arrière, sans écrire dans `campaigns` | Majeure | Cahier de recettes (R18) | `SupabaseService.createCampaign()` + génération d'un code d'invitation, navigation vers `RoomShell` avec l'id réel créé — commit `83ee877` |
| B14 | `RoomJoinPage` acceptait n'importe quel code non vide | Majeure | Cahier de recettes (R20) | `SupabaseService.getCampaignByJoinCode()` vérifie réellement le code contre `campaigns` avant de rejoindre — commit `83ee877` |
| B16 | `SettingsPage` existait mais n'était référencée dans aucune route | Mineure | Cahier de recettes (R28) | Route `/settings` ajoutée, icône Paramètres de `ProfilePage` branchée dessus — commit `83ee877` |
| B18 | `RoomShell` affichait toujours son contenu de démonstration (« Salle du Dragon ») quel que soit l'id de room reçu, y compris pour une vraie room créée via B13 ou rejointe via B14 | Majeure | Cahier de recettes (R18/R21) | `SupabaseService.getCampaignById()` + `AuthProvider.getCampaignById()` : `RoomShell` charge désormais la vraie campagne depuis `campaigns` via son `roomId` et affiche son titre/description/dernière mise à jour réels (au lieu du contenu codé en dur), avec un écran « room introuvable » si l'id ne correspond à aucune campagne — commit `40bd364` |
| B15 | `SupabaseService.uploadImage()` appelait `_readFile()`, qui levait `UnimplementedError` : toute tentative d'upload d'image plantait | Majeure | Fonctionnalité de stockage jamais terminée | `uploadImage()` prend désormais un `XFile` (image_picker) et lit ses octets via `readAsBytes()` — cross-platform, contrairement à `dart:io.File` qui aurait planté sur Flutter Web (même piège que B03). Branché dans `RoomCreatePage` : une icône importée depuis la galerie est réellement uploadée vers le bucket `images` sous `{userId}/room-icons/...` (conforme aux policies RLS déjà en place), et son URL publique devient `icon_url` de la room. `RoomShell`/`RoomHomePage` distinguent une icône de démo (asset) d'une icône uploadée (URL réseau, rendue via `Image.network`) — commit `bad5f2b` |
| B17 | Incohérence de nommage : la campagne « Mystères de l'Ombre » (carte d'accueil) devenait « Mystères du Ombre » dans le sous-titre du document associé | Cosmétique | Faute de frappe corrigée dans `HomePage` — commit `bad5f2b` |
| B19 | `HomePage` affichait toujours deux campagnes de démonstration codées en dur (« Mystères de l'Ombre », « La Quête du Dragon ») dans « Campagnes en cours », quel que soit l'utilisateur connecté — y compris s'il n'avait jamais créé ni rejoint aucune room. De plus, rejoindre une room par code (`RoomJoinPage`) ne persistait cette adhésion nulle part (ni en base, ni localement), donc une room rejointe disparaissait dès qu'on quittait l'écran | Majeure | Demande utilisateur | Nouvelle table `campaign_members` (migration + RLS) alimentée par `AuthProvider.joinCampaign()` lors d'un join par code ; `SupabaseService.getVisibleCampaigns()` combine les campagnes créées (`creator_id`) et rejointes (`campaign_members`) ; `HomePage` charge cette liste réelle via un `FutureBuilder` et n'affiche plus aucune carte si elle est vide (au lieu du contenu de démonstration) — commit `e0f8854` |
| B20 | Les liens de confirmation d'inscription et de réinitialisation de mot de passe envoyés par Supabase redirigeaient vers `http://localhost:3000` (Site URL par défaut du projet, pensée pour une app web) : sur mobile, ça atterrit sur une page qui ne répond rien, et le mot de passe ne peut jamais être réellement changé (le paramètre `code`/token reste coincé dans une URL que rien n'intercepte) | Majeure | Test manuel sur téléphone par l'utilisateur | Schéma d'URL custom `com.example.scriptoria://reset-callback/` enregistré côté natif (`AndroidManifest.xml`, `Info.plist`) et passé en `redirectTo` par `SupabaseService.resetPassword()` ; `MyApp` écoute `AuthChangeEvent.passwordRecovery` (via `AuthProvider.onAuthStateChange`) et ouvre le nouvel écran `ResetPasswordPage`, qui appelle `AuthProvider.updatePassword()` (déjà implémentée côté service mais jamais branchée) — commit `98573f2` |
| B21 | `HomePage` capturait la liste des campagnes une seule fois dans `initState()` : après avoir créé une room (navigation directe vers `RoomShell` sans repasser par l'accueil), revenir en arrière affichait toujours l'ancienne liste, donnant l'impression que la nouvelle room était inaccessible. Par ailleurs, le bouton "Supprimer la room" de `RoomSettingsPage` existait mais son `onTap` était vide, jamais branché | Majeure | Demande utilisateur (test manuel sur téléphone) | `HomePage` utilise désormais le pattern `RouteObserver`/`RouteAware` (`lib/core/navigation/route_observer.dart`, enregistré sur le `MaterialApp`) : `didPopNext()` recharge la liste à chaque retour sur l'accueil. La section "Campagnes en cours" a aussi une hauteur fixe et défile sur elle-même (`ListView` dans un `SizedBox`) au lieu de pousser le reste de la page. Suppression de room branchée : `SupabaseService.deleteCampaign()` (RLS `campaigns_delete_own` déjà en place), confirmation par `AlertDialog`, bouton visible uniquement pour le créateur (`RoomShell` calcule `isCreator`) — commit `77a4d74` |
| B22 | La section « Derniers documents modifiés » de `HomePage` affichait deux entrées codées en dur (« Carte du Royaume », « Personnages Importants »), avec un `onTap` vide, sur le tout premier écran vu après connexion. Contenu de démonstration résiduel de la même famille que B18, contredisant la documentation qui annonçait les données de démonstration éliminées | Mineure | Revue de l'état de la démonstration en vue de la soutenance | Section retirée de `HomePage`. Le widget `DocumentListItem` est conservé : il sera réutilisé pour lister les vraies notes de room |
| B23 | `friendlyAuthErrorMessage()` répondait « Une erreur est survenue. Vérifiez votre connexion internet et réessayez. » à **toute** erreur qui n'était pas une `AuthException` — y compris une erreur de base de données ou un bogue applicatif. La fonction était pourtant utilisée hors authentification (création/adhésion à une room, paramètres). Elle affirmait une cause jamais constatée, et a fait chercher un problème réseau là où il n'y en avait aucun (voir B24) | Majeure | Diagnostic d'un échec d'exclusion de joueur | Fonction renommée `friendlyErrorMessage()` et étendue aux `PostgrestException` (dont le refus RLS `42501` → « Vous n'avez pas les droits nécessaires ») et aux `StorageException`. La branche par défaut n'invente plus de cause : elle annonce une erreur inattendue et journalise le type et le message réels |
| B24 | L'exclusion d'un joueur affichait un message d'erreur alors que la suppression avait réussi en base : `_reload()` appelait `setState(() => _membersFuture = _loadMembers())`, forme fléchée qui **renvoie** la valeur affectée, donc un `Future` — ce que `setState` rejette. L'exception était de plus capturée par le `try` de l'exclusion, faisant passer un échec de rafraîchissement pour un échec de suppression | Majeure | Test manuel sur téléphone par l'utilisateur | Corps en bloc au lieu de la forme fléchée, et rechargement sorti du `try` : une erreur de rafraîchissement ne peut plus se faire passer pour un échec d'exclusion. Le test de non-régression vérifie désormais le rechargement de la liste **et** l'absence de message d'erreur, là où il se contentait de vérifier l'appel de suppression |
| B25 | Ouvrir un outil de room (Dé, Frise, Relations) faisait planter l'écran sur `Could not find the correct Provider<RoomProvider> above this DePage Widget`. `RoomShell` fournit le `RoomProvider` **sous** le Navigator : une route poussée par `Navigator.push` devient un frère du shell et non son descendant, et sort donc du provider. Le harnais de test ne pouvait pas le voir, car il montait les deux providers au-dessus du `MaterialApp` — position que l'application ne reproduit pas | Majeure | Test manuel sur téléphone par l'utilisateur | Fonction `pushRoomRoute()` qui réinjecte le `RoomProvider` dans la route poussée, utilisée par les trois outils. Surtout, le harnais de test place désormais `AuthProvider` au-dessus du `MaterialApp` et `RoomProvider` à l'intérieur de la route, exactement comme `main.dart` et `RoomShell` : le test échoue maintenant sur le code bogué, ce qui a été vérifié |
| B26 | La page Profil, atteignable en deux taps depuis l'accueil, affichait trois compteurs restés à zéro (« Campagnes », « Personnages », « Sessions ») et **trois onglets de contenu entièrement inventé** : campagnes « Les Chroniques d'Eldoria » et « Station Omega-7 », personnages « Lunaéa », « Thorn », « Iris-7 », et un historique de sessions qui n'a jamais existé. Aucune de ces données ne venait de la base. Le bouton « Modifier le profil » ne faisait par ailleurs que basculer une icône : `SupabaseService.updateUserProfile()` existait mais n'était appelée nulle part. L'écran journalisait enfin le profil complet de l'utilisateur via `print()` | Majeure | Revue de l'écran de compte, demandée par l'utilisateur | Page réécrite autour des seules données réelles : identité (avatar, nom affiché, `@identifiant`, email, bio), modification qui enregistre effectivement via `updateUserProfile`, et nombre de rooms — la seule statistique que l'application sait calculer. Les compteurs « Personnages » et « Sessions » sont retirés : aucun écran ne crée de personnage et la notion de session n'existe nulle part. Les `print()` sont supprimés |
| B27 | L'écran Paramètres, relié depuis le profil, affichait pour tout contenu le texte « Page Paramètres ». La route existait (correctif B16) mais ne menait à rien | Mineure | Revue de l'écran de compte, demandée par l'utilisateur | Écran doté des deux seules actions de compte réellement disponibles : changement de mot de passe (`AuthProvider.updatePassword`, avec la même règle de longueur qu'à l'inscription) et déconnexion. Rien d'autre n'y figure |

## Connus, pas encore corrigés

Aucun à ce jour.
