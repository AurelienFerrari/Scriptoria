import 'package:flutter/material.dart';

class RoomPlayersPage extends StatelessWidget {
  const RoomPlayersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Joueurs de la room'));
  }
}
